apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis-master
  namespace: forgeflow-redis
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: master
    app.kubernetes.io/part-of: forgeflow-v2
spec:
  serviceName: redis-master-service
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
      app.kubernetes.io/component: master
  template:
    metadata:
      labels:
        app.kubernetes.io/name: redis
        app.kubernetes.io/component: master
        app.kubernetes.io/part-of: forgeflow-v2
    spec:
      securityContext:
        fsGroup: 999
        runAsUser: 999
        runAsGroup: 999
      containers:
      - name: redis
        image: redis:7.2-alpine
        ports:
        - containerPort: 6379
          name: redis
        env:
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-auth
              key: redis-password
        - name: REDIS_ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-auth
              key: admin-password
        - name: REDIS_FORGEFLOW_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-auth
              key: forgeflow-password
        - name: REDIS_LOCK_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-auth
              key: lock-password
        - name: REDIS_TEAM_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-auth
              key: team-password
        - name: REDIS_MONITOR_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-auth
              key: monitor-password
        - name: REDIS_BACKUP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-auth
              key: backup-password
        - name: REDIS_METRICS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-auth
              key: metrics-password
        command:
        - redis-server
        - /etc/redis/redis.conf
        - --requirepass
        - $(REDIS_PASSWORD)
        - --aclfile
        - /etc/redis/acl.conf
        volumeMounts:
        - name: redis-config
          mountPath: /etc/redis/redis.conf
          subPath: redis.conf
        - name: redis-auth
          mountPath: /etc/redis/acl.conf
          subPath: redis-acl.conf
        - name: redis-data
          mountPath: /data
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "2Gi"
            cpu: "1"
        readinessProbe:
          exec:
            command:
            - redis-cli
            - --raw
            - incr
            - ping
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          exec:
            command:
            - redis-cli
            - --raw
            - incr
            - ping
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
      volumes:
      - name: redis-config
        configMap:
          name: redis-config
      - name: redis-auth
        secret:
          name: redis-auth
  volumeClaimTemplates:
  - metadata:
      name: redis-data
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: fast-ssd
      resources:
        requests:
          storage: 10Gi

---
apiVersion: v1
kind: Service
metadata:
  name: redis-master-service
  namespace: forgeflow-redis
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: master
    app.kubernetes.io/part-of: forgeflow-v2
spec:
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: master
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: redis-master
  namespace: forgeflow-redis
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: master
    app.kubernetes.io/part-of: forgeflow-v2
spec:
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: master
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  clusterIP: None  # Headless service for StatefulSet